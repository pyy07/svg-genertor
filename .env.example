# ============================================
# SVG 动画生成器 - 环境变量配置示例
# ============================================
# 复制此文件为 .env 并填写实际配置值
# cp .env.example .env

# ============================================
# 数据库配置
# ============================================
# PostgreSQL 数据库连接字符串
# 示例格式：
# - Vercel Postgres: postgresql://user:password@host:5432/database?sslmode=require
# - Supabase: postgresql://postgres:password@db.xxx.supabase.co:5432/postgres
# - 本地: postgresql://user:password@localhost:5432/svg_generator
DATABASE_URL="postgresql://user:password@localhost:5432/svg_generator?schema=public"

# ============================================
# AI Provider 配置
# ============================================
# 至少需要配置一个 AI Provider 的 API Key

# --- Google Gemini Provider ---
# Google AI Studio API Key
# 获取地址: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY="your-google-ai-api-key"

# Gemini 模型名称（可选，默认: gemini-2.0-flash-exp）
# 可用模型: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash, gemini-pro
GEMINI_MODEL="gemini-2.0-flash-exp"

# --- OpenAI Provider ---
# OpenAI API Key
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY="your-openai-api-key"

# OpenAI API Base URL（可选）
# 默认: https://api.openai.com/v1
# 用途：
#   - 使用代理服务: https://api.openai-proxy.com/v1
#   - 使用兼容 API: https://your-compatible-api.com/v1
#   - 本地测试: http://localhost:8080/v1
OPENAI_BASE_URL="https://api.openai.com/v1"

# OpenAI 模型名称（可选，默认: gpt-4o）
# 可用模型: gpt-4o, gpt-4-turbo, gpt-4, gpt-3.5-turbo
OPENAI_MODEL="gpt-4o"

# 默认使用的 AI Provider（可选）
# 可选值: gemini, openai
# 如果不设置，将自动选择第一个已配置的 Provider
AI_PROVIDER="gemini"

# ============================================
# NextAuth 配置
# ============================================
# NextAuth 密钥（用于加密 session）
# 生成方式: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-key"

# 应用访问地址
# 开发环境: http://localhost:3000
# 生产环境: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# 微信登录配置（可选）
# ============================================
# 微信开放平台 AppID
# 网站应用: 从 https://open.weixin.qq.com/ 获取
# 测试号: 从 https://mp.weixin.qq.com/debug/cgi-bin/sandboxinfo 获取
WECHAT_APP_ID="your-wechat-app-id"

# 微信开放平台 AppSecret
WECHAT_APP_SECRET="your-wechat-app-secret"

# 微信授权回调地址
# 必须与微信开放平台配置的授权回调域名一致
# 格式: https://your-domain.com/api/auth/wechat
WECHAT_REDIRECT_URI="https://your-domain.com/api/auth/wechat"

# 是否使用微信测试号（可选，默认: false）
# 测试号只支持在微信客户端内打开链接授权，不支持 PC 端扫码登录
WECHAT_USE_TEST_ACCOUNT="false"

# ============================================
# 开发/测试配置
# ============================================
# 允许匿名访问（仅用于本地开发测试）
# 设置为 true 时，无需登录即可生成 SVG
#  生产环境请勿启用
ALLOW_ANONYMOUS="false"

# ============================================
# 代理配置（可选）
# ============================================
# 如果需要通过代理访问外部 API（如 Google API、OpenAI API）
# 格式示例：
#   HTTP 代理: http://127.0.0.1:7890
#   HTTPS 代理: https://127.0.0.1:7890
#   SOCKS5 代理: socks5://127.0.0.1:7890
HTTPS_PROXY=""
HTTP_PROXY=""
PROXY=""

# ============================================
# 环境变量说明
# ============================================
# 必填项：
#   - DATABASE_URL
#   - 至少一个 AI Provider 的 API Key (GOOGLE_AI_API_KEY 或 OPENAI_API_KEY)
#   - NEXTAUTH_SECRET
#   - NEXTAUTH_URL
#
# 可选项：
#   - 微信登录相关配置（如需使用微信登录）
#   - AI Provider 模型配置（使用默认值即可）
#   - 代理配置（如无需代理可忽略）
#
# 注意事项：
#   1. 所有包含敏感信息的变量不要提交到 Git
#   2. 生产环境请使用安全的密钥生成方式
#   3. 确保数据库连接字符串包含正确的 SSL 配置
#   4. OpenAI BASE_URL 可用于使用代理或兼容 API 服务
